---
description: このリポジトリでは作業後に確認せず GitHub へ add / commit / push する
alwaysApply: true
---

# GitHub 自動反映

このリポジトリでファイルを作る・直したら、作業の最後に必ず次を実行する。

1. `PROJECTS.md` 末尾の「作業ログ」へ、日付付きで作業要点を1行追記する
2. `git add`（変更ファイルをステージする）
3. `git commit`
4. `git push origin main`

「上げてよいか」を毎回聞かない。このリポジトリでは上げてよい。

## 絶対にコミットしないもの

- `.env`、APIキー、トークン、パスワード、口座番号
- 患者・利用者を特定できる情報
- その他の秘密情報

該当しそうな内容があればコミットせず、ファイルから除く。
