# Repository agent rules

このリポジトリでは、ユーザーの明示指示を最優先する。日本語で簡潔に報告する。

## ゴール

依頼された変更を、必要最小限の差分で完成させる。実装だけで止まらず、影響範囲に応じた確認まで行い、レビュー可能な状態で返す。

## 自律して進めてよい範囲

- 関連ファイルの読み取り、調査、実装、可逆的な修正
- 必要なテスト、ビルド、表示確認
- 新規ブランチ作成、commit、push、PR作成
- 独立作業の並列化や補助エージェント利用（速度・品質向上が見込める場合）

細かい途中確認は不要。結果を左右する曖昧さが残る場合だけ質問する。まず安全に進められる作業を完了し、具体的な成果物を作る。

## 完了条件

- 依頼内容が反映されている
- 変更差分が依頼範囲に収まっている
- 影響範囲に応じた必要最小限の確認が通っている
- 失敗が今回の変更に起因する場合は修正して再確認している
- 最後に、変更内容・確認結果・未確認点を簡潔に報告する

小変更で無関係な広範囲テストを増やさない。新しい失敗や未解決の懸念がある場合だけ検証範囲を広げる。

## 承認が必要な境界

次は実行前に明示的な承認を得る。

- PRのマージ、mainへの直接反映
- 本番デプロイ・公開
- 外部サービスへの送信や投稿
- 課金、削除、権限変更など不可逆または影響の大きい操作

承認待ちになる場合も、その直前までの実装・検証・レビュー可能な成果物作成は先に完了する。

## GitHub運用

- 原則、新規ブランチで変更しPRを作る。mainへ直接pushしない。
- PR作成までは追加確認なしで進めてよい。
- マージはユーザーの明示承認後のみ。自動マージは使わない。
- 無関係なファイルを変更しない。

## 秘密情報

APIキー、トークン、パスワード、口座情報、患者・利用者を特定できる情報などの秘密情報をコミットしない。認証情報は環境変数やSecretを使う。

## 指示ファイルの扱い

- AGENTS.mdは恒久的な境界と完了条件だけを書く。
- 手順が特定作業だけに必要なら、READMEや専用ドキュメントへ分離する。
- 古い運用記録や停止前の手順を現行指示として残さない。
- 競合する指示がある場合は、ユーザーの最新の明示指示を優先する。

## 停止中の運用

### 個人予想（中央競馬・地方競馬・競艇）

Cursor / GitHub Actions による自動運用は停止中。`PERSONAL_PREDICT_ENABLED=false`。明示的な再開指示があるまで実行しない。詳細は `個人予想/AGENTS.md`。

### 競輪予想

Cursor連携と提出用自動化は停止中。`keirin-submit` / `keirin-ingest` を実行しない。明示的な再開指示があるまで競輪関連の自動処理を行わない。詳細は `競輪予想/AGENTS.md`。
